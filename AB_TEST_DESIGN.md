# A/B 实验设计：高价商品的转化干预

> 配套文档：[PROJECT_SUMMARY.md](PROJECT_SUMMARY.md)
> 目的：把分析结论中最关键的一条转化为可上线验证的实验方案

---

## 1. 为什么要做这个实验

分析给出的核心建议是：**$300 以上的高价商品，应改用不侵蚀毛利的转化手段，而非直接降价。**

这条建议背后是一个**尚未被验证的因果判断**：价格断崖反映的是「高价商品缺乏购买信心」，而不是「价格绝对值过高」。两种解释指向完全不同的动作——

- 若是**价格问题** → 降价有效，但直接吃掉毛利
- 若是**信心问题** → 分期、延保、以旧换新同样有效，且几乎不吃毛利

**观察性数据无法区分这两种解释。** 弹性回归虽然用商品固定效应排除了商品构成偏差，但无法排除随时间变化的混淆因素（如降价与站内推广同时发生），因此弹性估计只是方向性参考。必须靠随机化实验来识别真正的因果效应。

---

## 2. 假设

**H₀**：对 $300 以上的智能手机，分期免息与直接降价对购买转化率的提升无显著差异，且两者相对现状均无提升。

**H₁**：至少一种干预显著提升购买转化率；且若分期免息的提升幅度不显著低于直接降价，则支持「信心问题」而非「价格问题」的解释。

**这个设计的价值在于**：它同时检验了弹性模型的预测（降价组的实际提升是否接近模型预测的 +11.8%），以及建议 3 的商业主张（不侵蚀毛利的手段能否达到同等效果）。

---

## 3. 实验设计

### 3.1 实验对象

浏览过均价 $300 以上智能手机的用户。平台日均约 **2.77 万人**符合条件。

### 3.2 分组

| 组 | 干预 | 单位毛利影响 |
|---|---|---|
| **对照组 C** | 现状，原价展示 | — |
| **实验组 A** | 直接降价 10% | 毛利率直接减少 10 个百分点 |
| **实验组 B** | 12 期分期免息（价格不变） | 仅承担分期手续费，约售价的 3–5% |

流量按 1:1:1 分配。

**为什么设两个实验组而不是一个？** 只做「降价 vs 现状」只能验证降价有效，无法回答「有没有更省钱的替代方案」。A 与 B 的对比才是这个实验真正的商业价值所在。

### 3.3 分流单位：用户级

**必须按 `user_id` 分流，不能按会话或页面分流。** 三个原因：

1. **定价实验的一致性要求**。同一用户在不同会话看到不同价格，会直接损害信任并引发投诉，是定价实验的红线。
2. **避免污染**。用户跨会话比价会使组间差异被稀释，低估真实效果。
3. **指标口径一致**。核心指标以用户为分母（与分析中的漏斗口径一致），分流单位必须与指标单位相同，否则会低估方差、高估显著性。

分流方式：对 `user_id` 做哈希后取模分桶——与本项目抽样使用的方法一致，保证同一用户始终落在同一组。

### 3.4 实验时长

**至少 2 个完整自然周（14 天）。** 依据来自本项目 Day 1 的发现：平台流量存在明显的周内规律，周五至周日日均事件量比周四高约 14%，且周末用户人均浏览商品数更多（6.8 vs 工作日 6.3）。

实验必须覆盖**整数个自然周**，否则组间会因为周期不对齐产生系统性偏差。样本量在 2 天内即可满足，但仍须跑满 14 天以消除周期效应与新奇效应。

---

## 4. 指标

### 4.1 核心指标

| 指标 | 定义 | 说明 |
|---|---|---|
| **购买转化率** | 实验期内完成购买的用户数 ÷ 进组用户数 | 主指标，用于统计判读 |
| **人均 GMV** | 实验期内该组 GMV ÷ 进组用户数 | 转化率与客单价的综合结果 |
| **补贴 ROI** | 增量 GMV ÷ 干预成本 | 直接对应分析中 ROI 0.30 的结论 |

### 4.2 护栏指标

护栏指标的作用是**防止「赢了 GMV 但输了生意」**。任一护栏显著恶化，即使主指标胜出也不应推全量。

| 护栏指标 | 监测什么 | 恶化阈值 |
|---|---|---|
| **毛利率** | 干预是否吃掉了比换来的 GMV 更多的利润 | 相对下降 > 2% |
| **客单价** | 用户是否被引导买了更便宜的型号 | 相对下降 > 3% |
| **跨品类销量** | **替代效应**——手机的促销是否挤压了平板、笔记本 | 相对下降 > 3% |
| **老用户留存** | 是否让老用户形成「等打折」预期 | 次月复购率相对下降 > 3% |
| **退货率** | 分期是否诱发冲动购买后退货 | 相对上升 > 10% |

> **跨品类销量这一条是刻意加的。** 优化模型假设品类间需求独立、忽略替代效应，因此模型算出的 GMV 增量是**上界**。这个护栏指标就是用来实测这个假设错得有多离谱。

---

## 5. 样本量估算

### 5.1 参数

| 参数 | 取值 | 依据 |
|---|---|---|
| 基线转化率 p₁ | **7.95%** | 实测：42,867 名高价手机浏览用户中 3,410 人下单 |
| 最小可检测提升 MDE | **相对 +10%**（7.95% → 8.75%） | 弹性模型预测降价 10% 带来销量 +11.8%，取略保守的 10% |
| 显著性水平 α | 0.05（双尾） | 常规 |
| 检验效能 power | 0.80 | 常规 |

### 5.2 计算

两比例检验的每组样本量：

```
n = 2 · (z_{α/2} + z_β)² · p̄(1 − p̄) / δ²
```

代入 z₀.₀₂₅ = 1.96、z₀.₂₀ = 0.84、p̄ = 8.35%、δ = 0.795 个百分点：

```
n = 2 × (1.96 + 0.84)² × 0.0835 × 0.9165 / 0.00795²
  = 2 × 7.84 × 0.07649 / 0.0000632
  ≈ 18,975 人/组
```

**三组合计约 57,000 人。** 按日均 2.77 万符合条件用户计算，**约 2 天即可积累足够样本**。

### 5.3 若要检测更小的效应

| MDE（相对） | 每组样本量 | 所需天数 |
|---|---|---|
| +10% | ~19,000 | ~2 天 |
| +5% | ~74,300 | ~8 天 |
| +3% | ~206,000 | ~22 天 |

样本量与 MDE 的平方成反比。本实验按 14 天跑满，可支持到约 **+4% 的相对提升**——足以覆盖模型预测的效应量。

---

## 6. 判读规则

**主指标**：A vs C、B vs C 两个对比，各自做双比例 z 检验，用 Bonferroni 校正把单次显著性水平调整为 α = 0.025，控制多重比较带来的假阳性。

**关键对比**：A vs B 用等价性检验而非差异性检验——我们要回答的不是「B 是否优于 A」，而是「**B 是否不劣于 A**」。若 B 相对 A 的转化率差异的置信区间下界高于 −2 个百分点，即认定 B 不劣于 A。

**决策矩阵**：

| 结果 | 解释 | 行动 |
|---|---|---|
| A 显著 且 B 不劣于 A | 信心问题为主，分期同样有效且更省毛利 | **推 B 全量**，A 不上线 |
| A 显著 且 B 显著劣于 A | 确为价格敏感，弹性估计得到验证 | 按优化模型的补贴方案小规模执行，仅限高弹性品类 |
| A、B 均不显著 | 断崖由价格与信心之外的因素驱动 | 回到定性研究：用户调研、页面漏斗埋点细分 |
| 任一护栏显著恶化 | 赢了 GMV 输了生意 | **不推全量**，无论主指标结果 |

---

## 7. 可能的偏差与应对

| 偏差 | 机制 | 应对 |
|---|---|---|
| **新奇效应** | 用户对新出现的分期入口短期高频点击，效果虚高 | 跑满 14 天；分别看第 1 周与第 2 周效果，若衰减明显则以第 2 周为准 |
| **分期资格选择偏差** | 并非所有用户都能通过分期审核，实际接受干预的人群与随机分组人群不一致 | 主分析用 **ITT（意向治疗）** 口径，即按分组而非按实际使用分期计算；另做 LATE 分析作为补充 |
| **跨品类挤出** | 手机促销挤压平板、笔记本销量，总 GMV 未真正增长 | 跨品类销量列为护栏指标；主指标同时报告「全站人均 GMV」而非仅手机品类 |
| **比价与等待效应** | 用户观察到降价后推迟购买以等待更大折扣 | 监测对照组的转化率是否随实验推进而下降；实验期内不叠加其他促销 |
| **溢出效应** | 用户间口碑传播使对照组也知晓折扣 | 分流哈希与既有营销分桶错开；监测对照组转化率是否异常上升 |
| **样本比率不匹配（SRM）** | 分流实现有 bug 导致三组人数显著不等 | 每日做卡方检验，p < 0.001 即停止实验排查 |

---

## 8. 这份方案与分析结论的对应关系

| 分析结论 | 本实验如何验证 |
|---|---|
| $300 价格断崖（Finding 2） | 实验对象即限定为 $300 以上高价手机 |
| 手机弹性 −1.18（Finding 3） | 实验组 A 的实际提升若接近 +11.8%，则弹性估计得到验证 |
| 补贴 ROI 仅 0.30（Finding 4） | 核心指标包含补贴 ROI，直接实测 |
| GMV 方案损失 18.5% 毛利（Finding 5） | 毛利率列为护栏指标 |
| 建议改用非价格手段（Recommendation 3） | A vs B 的等价性检验即为该建议的直接检验 |
| 优化模型忽略替代效应（Limitation） | 跨品类销量列为护栏指标，实测假设偏差 |
